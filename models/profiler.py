# from pandas_profiling import ProfileReport
# import logging
# logger = logging.getLogger('mlt.profiler')

# class Profiler:
# 	def __init__(self, name, df):
# 		logger.info("Starting profiling ...")
# 		profile = ProfileReport(df, title=name, explorative=True, progress_bar=False)
# 		profile.to_file(f"{name}.html")
# 		logger.info("Done profiling!")
